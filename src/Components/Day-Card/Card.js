import React from 'react';
import './Card.scss';

const Card = (props) => {
    return(
        <React.Fragment>
            <div class="card-container">
                Sample Card
            </div>
        </React.Fragment>
    );
}

export default Card;